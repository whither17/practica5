#ifndef MACROS_H
#define MACROS_H

#include <QPoint>

#define sprites_weight 32 // tamaño de los esprites
#define sprites_height 32
#define width_ 448
#define height_ 564

const QPoint Up = QPoint(0, -1);
const QPoint Down = QPoint(0, 1);
const QPoint Left = QPoint(-1, 0);
const QPoint Right = QPoint(1, 0);



#endif // MACROS_H
